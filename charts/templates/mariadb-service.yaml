apiVersion: v1
kind: Service
metadata:
  name: mariadb
spec:
  selector:
    app: mariadb
  ports:
    - port: {{.Values.mariadb.service.port}}
      targetPort: {{.Values.mariadb.service.port}}
