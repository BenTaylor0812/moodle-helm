apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: moodle-code
  annotations:
    "helm.sh/resource-policy": keep

spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{.Values.moodle.storageMoodle}}